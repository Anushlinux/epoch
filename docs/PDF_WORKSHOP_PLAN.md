# PDF workshop assignment

Implement on `testing`, preserving the existing dirty checkout and historical CSV evidence.
The unchanged direction SHA-256 is `791826322bab72f3198c862cad796e2c5298c1ed5801fb8db8bd70a6101e3df5`.

1. Add scoped immutable PDF assets and a restricted Docker PDF runtime.
2. Expose an actually broken pagination tool through ordinary Hermes discovery.
3. Replace active CSV chat controls with PDF upload, preview and evidence controls;
   hide retired CSV chats without deleting their records.
4. Generate renderer repairs and a previously absent PDF merge tool with Luna.
   Protect criteria, verify isolated component/original/fresh/regression behavior,
   and publish only the exact verified source/bundle/image.
5. Prove actual Hermes recovery, fresh-chat reuse, restart and rollback. Retain all
   failed attempts. No push, merge, video or unsupported live claims.

Explicit investigation remains read-only. Fix tool/Create tool authorizes bounded
generation, verification, publication and Hermes recovery. Each action is limited
to two candidates, 60 provider requests and 1,800 seconds. Tools are project-scoped;
input files remain conversation-scoped. The PDF environment cannot use host shell,
network, provider credentials or arbitrary files.

Completion requires recorded live evidence; implementation and mocked tests alone
do not establish acceptance. See docs/status.md for demonstrated results.

All seven required live sequences have now passed, including a separate-process
restart check and isolated two-step rollback. Generated source, unsuccessful attempts,
protected checks and actual output PDFs are documented in
[the acceptance record](../backend/fixtures/pdf/README.md). See
[current status](status.md) for test results and the unrelated existing release-test
failure. Changes remain on `testing`; no push or merge was performed.
