# Verified CSV repair

Assigned September 6: extend the CSV debugger from diagnosis to an applied,
persistent environment repair. This supersedes the diagnosis-only CSV scope.

1. Preserve existing uncommitted work and the original CSV, service contract,
   criteria, Hermes implementation and discovery interface.
2. Let Luna propose a declarative adapter field mapping from captured failure
   evidence. Host code interprets this restricted artifact; no generated Python,
   shell, network access, or healthy-control switch is permitted.
3. Stage and verify the candidate against component cases, the original task with
   Hermes, a fresh task with Hermes, invalid inputs and duplicate handling. Keep
   candidate artifacts, rejected attempts and evidence outside the live sandbox.
4. Publish verified versions per project at the end of verification. Refresh the
   tool description through ordinary discovery, continue Hermes, and retain the
   original rejected receipt. New conversations use the persisted mapping.
5. Keep triggering explicit through the existing Debugger action. This action now
   authorizes bounded CSV repair and recovery; standard chat analysis is unchanged.
   Distinguish repair publication from original-task completion. Retain versions
   for host rollback and prove reuse/restart with focused tests.

Bounded scope: only the fixed CSV sample with a captured `missing_email` service
failure and no successful/partial writes is eligible. Unknown failures remain
diagnoses. One proposal, two isolated Hermes checks and one live recovery maximum;
each Hermes call is bounded to 20 turns/300 seconds. No new settings or credentials.
Live model acceptance and deterministic checks must be reported separately.

## September 7: explicit frontend execution

The user reported a diagnosis without an execution control. The running server
still exposed no CSV repair routes although frontend files had refreshed.

1. Separate diagnosis from execution: Investigate saves analysis; Verify and apply
   fix submits an explicit, idempotent CSV repair operation using current evidence.
2. Expose host eligibility/reasons in the environment response. Older servers
   must show a restart message, not promise repair support.
3. Show live verification, publication and recovery results alongside the action.
   Preserve rejected attempts, original criteria and all saved conversations.
4. Test backend gating, request recovery, browser controls and old-server behavior;
   restart the idle local service with its existing configuration and verify its
   advertised capability. Do not start model execution merely by opening the page.

## Rejected candidate recovery

The user's actual repair proposed the same faulty outgoing field and failed its
component checks. Nothing was published, but the operation was labeled completed.

1. Keep trusted rejection. Never activate the known-bad mapping or bypass checks.
2. Supply the captured source, outgoing payload and service-required fields as
   explicit mapping evidence. Feed rejected candidate/check results back to Luna.
3. Allow at most two candidates per captured failure, including historical
   attempts. Retry a failed component candidate once automatically; stop on
   uncertain executor outcomes, missing evidence, cancellation or exhausted limits.
4. Report failed verification as a failed operation. Distinguish publication from
   task recovery, and label the unchanged older analyses as historical.
5. Verify the real affected conversation, later-chat reuse, and retained failure
   evidence after focused tests. Preserve existing customer data and receipts.
