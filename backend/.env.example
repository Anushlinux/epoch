# Copy to .env and pass --env-file .env explicitly. No model credentials are needed.
EPOCH_HOST=127.0.0.1
EPOCH_PORT=8000
EPOCH_LOG_LEVEL=info
EPOCH_DATA_DIR=data
EPOCH_CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
