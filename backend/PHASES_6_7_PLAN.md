# Phases 6 and 7 development plan

User assignment: implement both phases and push to main quickly; do not run tests.
This overrides the normal pre-push testing workflow, not runtime publication gates.
Frontend files and original direction remain unchanged. Start from main `28d922a`.

1. Extend immutable environment versions to compose serializer, generated directory
   adapter and generated retrieval policy without dropping prior repairs.
2. Add evidence-based missing-capability/context investigation, generated source
   and contracts, permission-scoped discovery and isolated execution.
3. Extend runtime verification for negative directory cases, cross-project use,
   current/historical context, source preservation and prior surface regressions.
4. Retain budgets, original/fresh Hermes gates, safe activation and rollback;
   expose additive API/CLI metadata and update integration documentation.
5. Export contracts, inspect the Git change scope, commit and push. Do not execute
   pytest, lint checks, model calls, Docker probes or acceptance demos. Report the
   implementation as untested; no new acceptance or compatibility claims.
